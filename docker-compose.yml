version: '3.8'

services:
  app:
    build: ./
    ports:
      - '443:443'
    volumes:
      - /home/azure/biocloudlabs.cer:/etc/ssl/certs/biocloudlabs.cer
      - /home/azure/biocloudlabs.key:/etc/ssl/private/biocloudlabs.key